
version: 2

models:
    - name: dim_emp_comp_range
      description: "A starter dbt model"
      columns:
          - name: EMP_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: dim_employee
      description: "A starter dbt model"
      columns:
          - name: EMP_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
